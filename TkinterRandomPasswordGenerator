
import tkinter as tk
import random

def game(user_action):
    possible_actions = ["rock", "paper", "scissors"]
    computer_action = random.choice(possible_actions)

    if user_action == computer_action:
        result = ("Both players selected", user_action, "It's a tie!")
    if user_action == "rock":
        if computer_action == "scissors":
            win_result = ("Rock smashes scissors! You win!")
            win_result= tk.Label(window, text="", bg="green", fg="white")
        else:
            result = ("Paper covers rock! You lose.")
    if user_action == "paper":
        if computer_action == "rock":
            result = ("Paper covers rock! You win!")
            win_result = ("Rock smashes scissors! You win!")
            win_result= tk.Label(window, text="", bg="green", fg="white")

        else:
         result = ("Scissors cuts paper! You lose.")
    if user_action == "scissors":
        if computer_action == "paper":
            result = ("Scissors cuts paper! You win!")
            win_result = ("Rock smashes scissors! You win!")
            win_result= tk.Label(window, text="", bg="green", fg="white")

        else:
            result = ("Rock smashes scissors! You lose.")
    result_label.config(text = "You chose:"+ user_action+ "\n Computer chose:"+ computer_action)
            
window = tk.Tk()
window.title("Rock Paper Scissors Game")
window.geometry("600x600")
window.config(bg="yellow")
result_label=tk.Label(window, text="", bg="green", fg="white")
result_label.pack(pady=20)
title_label = tk.Label(window, text="Rock Paper Scissors Game!", bg = "green", fg="white")
title_label.pack(pady=10)
rock_button = tk.Button(window, text="Rock", command=lambda: game("rock"))
rock_button.pack(pady=10)

paper_button = tk.Button(window, text="Paper", command=lambda: game("paper"))
paper_button.pack(pady=10)

scissors_button = tk.Button(window, text="Scissors", command=lambda: game("scissors"))
scissors_button.pack(pady=10)

window.mainloop()
